export const metadata = {
  title: 'Store Admin | ApexBlueSky',
  description: 'Manage your digital product storefront.',
};

export default function StoreAdminLayout({ children }: { children: React.ReactNode }) {
  return children;
}
